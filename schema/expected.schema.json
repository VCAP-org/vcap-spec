{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "expected.json of a conformance vector",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "kind"
  ],
  "properties": {
    "kind": {
      "enum": [
        "file",
        "container",
        "segments",
        "jcs"
      ]
    },
    "outcome": {
      "enum": [
        "authentic",
        "verified_clip",
        "tampered",
        "nested_proof",
        "corrupted_proof",
        "no_proof_found",
        "unsupported_format_version"
      ]
    },
    "labels": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "not_evaluated": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "core_hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "segments": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "verified"
      ],
      "properties": {
        "verified": {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 0
          }
        }
      }
    },
    "schema_valid": {
      "type": "boolean",
      "description": "file and container vectors: whether proof.json conforms to vcap-proof-1.0.schema.json"
    },
    "core_bytes_hex": {
      "type": "string",
      "pattern": "^([0-9a-f]{2})+$"
    },
    "debug": {
      "type": "object"
    },
    "level": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "claimed",
        "proven",
        "ceiling"
      ],
      "description": "\u00a77: the level claimed by the device, the level the evidence proves, and the ceiling the two allow.",
      "properties": {
        "claimed": {
          "type": "string"
        },
        "proven": {
          "type": "string"
        },
        "ceiling": {
          "enum": [
            "green",
            "amber",
            "red"
          ]
        }
      }
    },
    "validated_at": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "instant",
        "source"
      ],
      "description": "\u00a77: the instant every certificate path was validated at, and what proved it.",
      "properties": {
        "instant": {
          "type": "string"
        },
        "source": {
          "enum": [
            "timestamp",
            "anchor",
            "device_clock",
            "verifier_clock"
          ]
        }
      }
    },
    "verifier_clock": {
      "type": "integer",
      "minimum": 0,
      "description": "An input, not an expectation: the verifier's own clock, ms. A \u00a77 verdict depends on it \u2014 a chain expires \u2014 so a vector that left it to the calendar would change its own answer over time."
    },
    "key_status": {
      "type": "object",
      "description": "An input, not an expectation: \u00a76.2's online key status as the verifier is assumed to have fetched it. Absent means the verifier could not ask, which is *revocation not checked* and caps the verdict at amber.",
      "additionalProperties": false,
      "required": [
        "log_id",
        "at",
        "tree_size",
        "status",
        "signature"
      ],
      "properties": {
        "log_id": {
          "type": "string"
        },
        "at": {
          "type": "integer",
          "minimum": 0
        },
        "tree_size": {
          "type": "integer",
          "minimum": 1
        },
        "status": {
          "type": "integer",
          "enum": [
            0,
            1,
            2
          ]
        },
        "signature": {
          "type": "string"
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "type": "object",
        "properties": {
          "kind": {
            "const": "jcs"
          }
        }
      },
      "then": {
        "type": "object",
        "required": [
          "core_bytes_hex",
          "core_hash"
        ]
      }
    },
    {
      "if": {
        "type": "object",
        "properties": {
          "kind": {
            "enum": [
              "file",
              "container",
              "segments"
            ]
          }
        }
      },
      "then": {
        "type": "object",
        "required": [
          "outcome",
          "labels",
          "not_evaluated"
        ]
      }
    }
  ]
}
