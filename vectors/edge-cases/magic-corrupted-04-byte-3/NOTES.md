# magic-corrupted-04-byte-3

Byte 3 of "VCAP" flipped (0xff XOR), the other 3 magic bytes and the rest of the footer left alone. §3 requires all four magic bytes to match before anything else is read, so one wrong byte anywhere in the magic reads the same as no trailer at all: *no proof found*.

Generated by `tools/src/generate-edge-cases.ts` (seed 1) with the test key in `tools/src/testkey.ts`.
