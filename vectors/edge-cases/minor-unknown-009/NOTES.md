# minor-unknown-009

`v` is `vcap/1.9` with one unknown top-level key, `future_field_9`. Same major: §9 says verify what you know and list the rest as not evaluated, whatever the minor number is — this is vector 15's case swept across the minor range instead of asserted once at 7.

Generated by `tools/src/generate-edge-cases.ts` (seed 1) with the test key in `tools/src/testkey.ts`.
