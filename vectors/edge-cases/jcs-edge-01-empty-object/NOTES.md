# jcs-edge-01-empty-object

The empty object: `{}`, two bytes. The floor every other JCS vector in this repository builds on top of, pinned on its own.

Generated by `tools/src/generate-edge-cases.ts` (seed 1).
