# required-missing-06-media-w

A sealed file whose proof is missing `media.w` (dropped after signing the full core, so the trailer still parses and the JSON is otherwise the vector's usual one). §6.1/§8 require it; the reference verifier's shape check catches its absence before the signature is even read, the same *no proof found* an unparseable payload gets. Schema-invalid for the same reason.

Generated by `tools/src/generate-edge-cases.ts` (seed 1) with the test key in `tools/src/testkey.ts`.
